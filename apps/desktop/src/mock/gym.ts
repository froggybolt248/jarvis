/** Static mock data. Swap for a real workout-log source once the backend lands. */
export const gymStatus = {
  scheduled: "Push Day scheduled",
  lastSession: "Pull, Tue",
};

/** Last 7 workout session durations (minutes), oldest to newest. */
export const sessionTrend = [42, 55, 38, 60, 48, 65, 52];
